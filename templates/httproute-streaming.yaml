{{- if or .Values.httproute.streamingHostnames .Values.httproute.streamingParentRefs -}}
{{- $fullName := include "mastodon.fullname" . -}}
{{- $streamingPort := .Values.mastodon.streaming.port -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $fullName }}-streaming
  labels:
    {{- include "mastodon.labels" . | nindent 4 }}
    {{- with .Values.httproute.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.httproute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- coalesce .Values.httproute.streamingParentRefs .Values.httproute.parentRefs | toYaml | nindent 4 }}
  hostnames:
    {{- coalesce .Values.httproute.streamingHostnames .Values.httproute.hostnames | toYaml | nindent 4 }}
  rules:
    {{- range .Values.httproute.streamingRules }}
    {{- with .matches }}
    - matches:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .filters }}
      filters:
      {{- toYaml . | nindent 8 }}
    {{- end }}
      backendRefs:
        - group: ''
          kind: Service
          name: {{ $fullName }}-streaming
          port: {{ $streamingPort }}
          weight: 1
    {{- end }}
{{- end }}
